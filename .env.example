PROJECT_ID=skeletonV2

# Identifies which servo_calibration/<CALIBRATION_ID>.json file this physical
# unit uses. Leave blank on a brand new unit - a UUIDv4 will be generated and
# saved here automatically the first time you calibrate it. "default" is the
# file seeded with the project's original hardcoded values.
CALIBRATION_ID=default