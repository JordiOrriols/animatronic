PROJECT_ID=skeletonV2

# Identifies which servo_calibration.json profile this physical unit uses.
# Leave blank on a brand new unit - it will be generated and saved here
# automatically the first time you calibrate it. "default" is the profile
# seeded with the project's original hardcoded values.
CALIBRATION_HASH=default